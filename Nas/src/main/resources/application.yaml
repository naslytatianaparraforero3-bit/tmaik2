server:
  port: 8080

spring:
  application:
    name: Nas
  datasource:
    url: jdbc:mysql://localhost:3306/futbol_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true